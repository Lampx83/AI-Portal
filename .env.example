# Copy to .env and set your values.
# - npm run dev: frontend (next.config.mjs) và backend (lib/env.ts) đều load file .env ở thư mục gốc (AI-Portal/.env).
# - Docker Compose: đọc .env để thay thế ${NEXTAUTH_SECRET} trong docker-compose.yml.
# NEXTAUTH_SECRET: secret để ký JWT (backend + frontend middleware). Production: dùng chuỗi ngẫu nhiên (vd. openssl rand -base64 32).
NEXTAUTH_SECRET=your-secret-here-change-in-production
