# Development: full source + tsx watch, mount ./src để hot reload
FROM node:20-alpine

WORKDIR /app

RUN apk add --no-cache wget curl postgresql-client

COPY package.json package-lock.json* ./
RUN npm install

COPY . .

EXPOSE 3001

ENV PORT=3001

CMD ["npm", "run", "dev"]
