services:
  backend:
    image: ${DOCKERHUB_USER:-yourusername}/ai-portal-backend:latest
    platform: linux/amd64
  frontend:
    image: ${DOCKERHUB_USER:-yourusername}/ai-portal-frontend:latest
    platform: linux/amd64
